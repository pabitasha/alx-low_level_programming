#include "main.h"
/**
 * _issupper check if letter is  uppercass
 *  @c:  check for char
 *
 *  Reyurn: if uppercase return1, else return 0
 *  */
int _issupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
	return (1);
}
  return (0);
